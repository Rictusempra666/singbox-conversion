name: Convert JSON to SRS

on:
  push:
    branches:
      - main
    paths:
      - '*.json'

jobs:
  convert-json:
    runs-on: ubuntu-latest

    steps:
      # Step 1: 检出仓库代码
      - name: Checkout Repository
        uses: actions/checkout@v3

      # Step 2: 下载 SingBox
      - name: Download SingBox
        run: |
          curl -L -o sing-box.tar.gz https://github.com/SagerNet/sing-box/releases/download/v1.10.3/sing-box-1.10.3-linux-amd64.tar.gz
          tar -xzf sing-box.tar.gz
          mv sing-box-1.10.3-linux-amd64/sing-box ./
          chmod +x sing-box

      # Step 3: 转换 JSON 文件
      - name: Convert JSON Files to SRS
        run: |
          for json_file in *.json; do
            echo "Converting $json_file..."
            ./sing-box json2src -c "$json_file" -o "${json_file%.json}.srs"
          done

      # Step 4: 提交生成的 SRS 文件
      - name: Commit and Push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add *.srs
          git commit -m "Auto-converted SRS files from JSON" || echo "No changes to commit."
          git push
